// Este solo llama el useColorScheme de ReactNative (Este archivo es el que se esta llamando en el "_layout")
// La razon de porque hacen esto es por que tienen implementado el Patron Adaptador
export { useColorScheme } from 'react-native';
